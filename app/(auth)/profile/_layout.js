//(auth)/profile/_layout.js


import React from 'react';
import { Slot } from 'expo-router';

export default function ProfileLayout() {
  // any shared UI (headers, nav) around your profile screens
  return <Slot />;
}